'use client';
import * as LabelPrimitive from '@radix-ui/react-label';
import styled from 'styled-components';

const Label = styled(LabelPrimitive.Root)`font-size: 0.88rem;`;
Label.displayName = 'Label';

export default Label;